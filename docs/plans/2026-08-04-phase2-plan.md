# 第二阶段开发目标计划（收尾工程 + 叙事增强 + 产品化）

> 日期：2026-08-04
> 状态：📋 待用户核对（核对通过后按本计划分步执行）
> 依据：全量排查（文档扫描 + 代码级终审，2026-08-04）：
> - `docs/audits/2026-08-03-production-gap-bug-inventory.md`（用户实测 bug 清单 §1/§3/§4）
> - `docs/audits/2026-07-30-code-audit.md`（M 级残留）
> - `docs/audits/2026-08-03-code-audit.md`（M 级 + §8.2 存疑，tier2/tier3 已修复项除外）
> - `docs/frontend-requirements.md` §12（A1-A12 生产验收清单）
> - 各 frontend-test-runs 测试轮文档 + frontend-bug-backlog.md
> - 代码级终审：src/app/routes-chat.ts、routes-ext.ts、routes-scheduler.ts、src/orchestrator/service.ts、src/event-queue.ts、frontend-demo/views/studio.js 等

## 〇、前提结论（已查证，非本阶段任务）

上一轮完成的存疑查证，结论直接作为已知事实：

| 存疑项 | 结论 | 证据 |
|---|---|---|
| auth.json chmod 600 | ✅ SDK 已设（目录 0o700 / 文件 0o600） | pi-ex `packages/coding-agent/src/core/auth-storage.ts:63,70,112,157`；引擎经 `AuthStorage.create`（unified-server.ts:150） |
| agent_end 必发 | ✅ 正常/异常/abort 三路径全覆盖 | pi-ex `packages/agent/src/agent.ts:453-495` + agent-loop.ts:198/249/268 |
| npm audit | ✅ CI 已加 `npm audit --omit=dev` | `.github/workflows/test.yml:69-74`（L-Test-7） |
| streamSimple 并发/限速 | ⚠️ 无内置限制，429 直接冒泡；引擎侧并发受控（EventQueue 串行 + Agent 单 run） | pi-ex `packages/ai/src/stream.ts:43-51`、`providers/openai-responses.ts:159-177` |

## 一、现状基线（第一阶段交付 + 真实缺口）

第一阶段（架构转型 → SDK 化 → 前端 7 页 → code-audit 🔴1-11 → 测试纪律）已完成并验证：测试 644 全绿、8 轮前端测试轮、novel 实盘编排全链路跑通（planner→role×6→reasoner→renderer，164s）。

**但全量排查发现：08-03 实测登记的 `production-gap-bug-inventory` 中约 14 项未收尾**（backlog BUG-001~009 只覆盖了该清单 §2 专项 4 项 + 后续轮次 5 项，§1 业务线扫描 16 项未建账）；另有 07-30 审计 M 级残留、验收签字未勾选、文档滞后等。本阶段即为此收尾 + 向前推进。

## 二、阶段目标定义

| 组 | 目标 | 优先级 | 交付物 |
|---|---|---|---|
| **G1 编排工作台健壮性** | 修复编排轮询/队列/前端语义缺口，消除"跑过就卡""toast 轰炸""结果看不到" | P0/P1 | 代码修复 + 测试轮 |
| **G2 会话管理落地** | 按 production-gap §3 方案实现会话新建/切换，消灭假会话 404 与"乱切" | P1 | 后端端点 + 前端真实现 + 测试轮 |
| **G3 审计残留清理** | 07-30 audit M 级 + 死代码 + 硬编码清理 | P2 | 代码修复 + 单测 |
| **G4 文档对齐与验收** | 消除文档滞后（CORS 军规/app-mode/状态标记）；执行 §12 A1-A12 生产验收 | P1 | 文档更新 + 验收登记 |
| **G5 叙事增强** | 记忆注入 + 提示词优化 + plan 持久化 + 子代理重试 | P2 | 设计 + 实现 |
| **G6 产品化** | Tauri sidecar 链路确认 + 跨平台验证 + 版本发布 | P2 | 打包验证 + 发布 |

## 三、任务分解（分步迭代，每步可独立验收）

### G1 编排工作台健壮性（P0 → P1，先做）

| # | 任务 | 现状证据 | 验收标准 |
|---|---|---|---|
| G1-1 | **status 轮询瘦身**：queueStatus 的 items 只返回摘要（queueId/status/storyTime/时间戳/error 消息/结果摘要），完整 result 改单条端点按需拉取 | service.ts:188 `...(q.result !== undefined ? { result: q.result } : {})` 全量挂载，每 2s 50KB+ | /api/scheduler/status 响应显著减小；前端 2s 轮询正常；单条详情端点可用 |
| G1-2 | **queue.length 语义**：EventQueue 增加 active（running/pending）计数接口；前端状态栏与轮询用 active，不再用累计 length | studio.js:279/1187 `status.queue.length` 当活跃数 | 跑过一次后状态栏回"空闲"，不再永远"N 个任务执行中" |
| G1-3 | **planDetail 轮询竞态容错**：stLoadPlanDetails 改 Promise.allSettled + 404 静默（plan 已被 commit/discard 属正常），不再每 2s toast 轰炸 | studio.js:94-98 裸 Promise.all | commit/discard 间隙轮询无错误 toast；正常流程无回归 |
| G1-4 | **demo 残留清理**：派发面板默认文案/默认 storyTime 改为空或项目真实值；characterIds 为空时给明确引导 | studio.js:415 硬编码「让艾莉亚在第七星港触发身世线索」、:427 默认 `ch006.ev008` | 新项目打开 studio 无艾莉亚/ch006 残留；空角色派发有引导 |
| G1-5 | **yolo 结果呈现**：yolo 完成后队列条目结果展示为独立卡片（章节路径/变更摘要），可跳转文件页 | 目前仅队列条目 + commitResult（plan 模式） | yolo 跑完有明确结果卡 |
| G1-6 | **队列错误可见化确认/补齐**：队列条目 error 在 studio 展示（含 retrieval_plan 非法参数场景） | collect.ts error 已入库；studio.js:497 有 st-tool-result-error | 派发失败可见错误文案，不"派发后什么都没发生" |
| G1-7 | **busy 对齐 isStreaming**：前端读 `/api/chat/status.isStreaming` 兜底 busy 状态（agent_end 之外的第二信号） | 前端从不读 isStreaming（production-gap §1.2） | 切走再回来，后台生成中不撞 409 CHAT_BUSY |
| G1-8 | **前端健壮性小项**：M-Collab-2（message_end 未处理→错误不标红）、M-Logic-8（navigate 替换 viewState 引用）、M-Logic-9（2s 轮询无退避）、M-Logic-11（events.js storyTime 竞态）、M-Logic-12（EventSource 无退避重连） | 08-03 audit M 级清单 | 各小项修复 + 无回归 |

### G2 会话管理落地（P1，G1 之后）

依据：`production-gap-bug-inventory.md` §3 落地方案（runtime 原语已查证：`newSession`/`switchSession`/`fork` 位于 pi-ex `packages/coding-agent/src/core/agent-session-runtime.ts:187-246`）。

| # | 任务 | 验收标准 |
|---|---|---|
| G2-1 | 后端：`POST /api/chat/sessions`（调 runtime.newSession）+ `POST /api/chat/sessions/:id/activate`（SessionManager.list 找 path → runtime.switchSession），envelope/错误码对齐既有契约 | 两端点 curl 可建/可切；会话落盘 `.pi/sessions/`；id 前缀解析 |
| G2-2 | 前端：`stNewSession` 真调用 POST；切换走 activate；live 对齐逻辑保留 | 点「新建议程」不再 404 清空消息；切旧会话发送进该会话（或明确只读语义） |
| G2-3 | host 启动恢复最近会话（按 production-gap §3 建议），治理"每次打开 studio 一个会话碎片" | 重启服务 + 打开 studio 不新增碎片会话 |
| G2-4 | 专项测试轮：会话新建/切换/恢复/碎片治理 全流程 | 测试轮文档全绿 |

### G3 审计残留清理（P2）

| # | 任务 | 现状证据 |
|---|---|---|
| G3-1 | novel-importer 修复：M4b（import-novel-v3 硬编码本机 Windows 路径）、M4c（resume 从阶段 ≥2 起章节全文丢失 → 0 事件） | 07-30 audit §M4b/M4c |
| G3-2 | admin 死代码：M17 stderrTail、M18 env-store filter 恒真、M19 _normalizeNovelJson 重复赋值 | 07-30 audit |
| G3-3 | 数据/工具层：M2（getAllDeclarationsAt 丢弃 valueText）、M6（Search embedder 强制非空反模式）、M10（debug 模块级计数器）、M13（InteractHooks 零测试）、M14（relation_update.target 设计张力——需决策） | 07-30 audit |
| G3-4 | Tauri/构建残留：M20（launchVisualizer sidecar 失效）、M22（Tauri 启动页端口 7421 硬编码）、M24（版本号三处不一致）、M25（lib.rs 注释错）、M27（.cargo 中国镜像）、M28（extract-fields.ps1 硬编码路径）、M29（tauri prebuild 不触发） | 07-30 audit |
| G3-5 | 编排提示词层：M-Qual-1（collect details undefined 静默 resolve）、M-Qual-5（串行角色注入多条 user message 被误读为多轮对话） | 08-03 audit |

### G4 文档对齐与验收（P1，可与 G1/G2 并行）

| # | 任务 | 现状证据 |
|---|---|---|
| G4-1 | frontend-requirements 军规 4 更新：CORS 已收紧（恶意 Origin 403），"允许跨域 *" 描述过时 | frontend-requirements.md:44 vs fix-audit-tier1 实测 |
| G4-2 | app-mode.md 同步：扩展模式/launch-pi/extension-snapshot/reinstall 已删，文档仍描述为现有能力 | app-mode.md 全文 |
| G4-3 | 状态标记更新：old-code-cleanup 报告"待实施"、superpowers UI 对齐计划勾选框、"A5 未做"（实际已做） | 各 plan/report 文档 |
| G4-4 | 执行 §12 A1-A12 生产验收（真 LLM + 浏览器逐项），登记结果 | frontend-requirements.md:389-406；handoff 签字未勾选 |
| G4-5 | 版本号统一（package.json 0.1.0-alpha.1 vs 模板 0.1.0） | M24 |

### G5 叙事增强（P2，G1-G4 稳定后）

| # | 任务 | 现状 |
|---|---|---|
| G5-1 | 记忆注入：基于 `.pi/world-graph-v3/memory.md` 已有产出，设计并实现主会话/子代理的前情注入（入口 `.pi/SYSTEM.md` 已预留，main-session.ts:11） | 08-01 全景图 P2 排期项 |
| G5-2 | 提示词优化：角色一致性（长程设定不漂移）、前情引用（避免重复铺垫） | 同上 |
| G5-3 | plan 持久化：进程重启不丢未 commit plan（plans Map → 落盘/恢复） | 08-01 data-layer-ports 报告 L151 |
| G5-4 | 子代理未提交产出重试机制（LLM 非确定性） | 08-01 orchestrator 报告 L90 |

### G6 产品化（P2）

| # | 任务 | 现状 |
|---|---|---|
| G6-1 | Tauri sidecar 打包链路确认（M-Collab-6 stdio + M29 prebuild） | 08-03 audit |
| G6-2 | 跨平台验证（macOS/Linux，app-mode 已知限制 5 / SETUP.md 均未实测） | 需真实环境 |
| G6-3 | 版本发布：0.1.0-alpha.1 → 正式版（含 CHANGELOG） | 当前 0.1.0-alpha.1 |

## 四、执行顺序与依赖

```
G1（编排健壮性，P0 先行：G1-1/2/3 → G1-4/5/6 → G1-7/8）
  │
  ├─→ G2（会话管理，依赖 G1 前端稳定）
  │
  ├─→ G4（文档对齐，可与 G1/G2 并行；G4-4 验收在 G1/G2 完成后做）
  │
  └─→ G3（审计残留，可并行，低风险）
        │
        └─→ G5（叙事增强，依赖 G3-3/G3-5 提示词层稳定）
              └─→ G6（产品化，依赖全部稳定）
```

- 每任务完成即跑全量测试（当前 644 基线）+ 涉及 frontend-demo 的按前端测试纪律自驱测试轮
- 每任务独立 commit（narrative-engine 分支策略：`20260804-<描述>` 分支 → ff-only 合并）
- G2 结束补会话专项测试轮；G4-4 结束登记 A1-A12 验收结果

## 五、验收总标准（第二阶段完成判据）

1. G1-G3 全部任务代码落地，`npm test` 全绿（644 基线不降）
2. G2 会话专项测试轮全绿（新建/切换/恢复/无碎片）
3. G4-4 A1-A12 生产验收逐项登记（含真实 LLM dispatch/chat + 10MiB 文件实测）
4. 前端测试轮文档齐备（修改 frontend-demo 必附，纪律硬约束）
5. 文档滞后批清零（CORS 军规 4 / app-mode / 状态标记 / 版本号）

## 六、明确不做（延续既有决策）

- novel 仓库清理与乱码数据修复（用户决策：novel 不进开发名单；SMOKE 乱码为 novel 数据问题，仅登记风险——引擎侧如需编码校验另议）
- 聊天 UI 独立页面（08-01 用户决策：只留 API）
- 伏笔机制/未来事件占位（07-25 audit 待决策，远期）
- 多用户/权限/鉴权（§13 本期不做）、云端部署
- 跨文件内容搜索、附件上传、提示词模板库（§13）

## 七、风险与存疑（执行中保持跟踪）

| 风险/存疑 | 影响 | 缓解 |
|---|---|---|
| SDK `newSession`/`switchSession` 边界行为（G2-1 依赖） | 会话实现返工 | 开工前读 agent-session-runtime.ts 源码二次查证（production-gap §3 已列 API，未实测边界） |
| streamSimple 无 429 退避（已查证） | role 并发限流时角色失败 | G1-6 错误可见化兜底；G5-4 重试机制后续补 |
| G4-4 真机验收依赖真实 LLM 密钥与较长执行时间 | 验收拖延 | 复用 novel 实盘（deepseek-v4-flash 已跑通）；A5/A6/A9 编排与轮转场景单独排时 |
| 会话碎片治理依赖 SDK 会话落盘时机 | G2-3 效果打折 | 实现时验证 SessionManager.list 空态；若 SDK 不可控则降级为"前端不主动建会话" |
| G5 记忆注入需 novel 实盘验证，但 novel 不进开发名单 | 效果验证受限 | 用测试项目（tests/fixtures）验证机制正确性，实盘效果由用户后续自测 |

## 八、排期建议（供参考）

| 阶段 | 内容 | 预计工作量 |
|---|---|---|
| 第一批 | G1-1/2/3（轮询三件套） | 1 轮 |
| 第二批 | G1-4/5/6/7/8 + G4-1/2/3（清理+文档） | 1 轮 |
| 第三批 | G2 会话管理（后端+前端+测试轮） | 1-2 轮 |
| 第四批 | G3 审计残留 + G4-4 验收 | 1-2 轮 |
| 第五批 | G5 叙事增强（设计先行） | 2+ 轮 |
| 第六批 | G6 产品化 | 视环境 |

---
**待用户核对**：以上目标定义、任务分解、优先级、不做范围、验收标准是否有需要调整的？核对通过后从 G1 第一批开始执行。

---

## 附录 A：执行进度登记

### 批次 1（编排骨架 + 同源安全）— 2026-08-04

- **分支**：`20260804-orch-skeleton-security`
- **范围调整**：用户决策（2026-08-04）将本阶段定位为"基干功能完整可用 + 健壮性"清扫，新功能（G2/G5/G6）暂不做。批次 1 在 phase2-plan 原料基础上补入 08-03 audit 5 项高危（🔴-2/3/4/5/6）。
- **实际改动**：
  - **本批新写代码（4 项）**：
    - G1-1：`src/orchestrator/service.ts` `queueStatus` 瘦身（移除 `result` 全量挂载，改 `resultSummary` 摘要）+ 新增 `active` 字段
    - G1-2：`src/event-queue.ts` 新增 `activeCount` getter；`frontend-demo/views/studio.js` 状态栏改用 `active`；`mock-data.js`/`api-mock.js` 同步
    - G1-3：`frontend-demo/views/studio.js` `stLoadPlanDetails` 改 `Promise.allSettled` + 404 静默
    - G1-7：`frontend-demo/views/studio.js` `stStartRealRuntime` 拉 `/api/chat/status.isStreaming` 兜底 busy；`api-client.js` 新增 `getChatStatus`；`api-mock.js` 加 mock 实现
  - **本批仅文档同步（11 项，代码在 master 已修但未文档化）**：
    - 🔴-2 commit 一致性缺口（`service.ts` catch 块已从 err 取 appliedEventIds，部分写入时保留 plan）
    - 🔴-3 子代理无超时（`collect.ts` `timeoutMs=180_000` 默认值已加）
    - 🔴-4 EventQueue 无界（`event-queue.ts` `maxLength=200` + `finishedTtlMs=1h` + `sweepFinished` 惰性清理已加）
    - 🔴-5 路径遍历（`path-guard.ts` `assertPathInside` + 4 处入口调用：`resolveChapterPath` / `chapter_read` / `chapter_write` / `parseCardFile` / `import-tools`）
    - 🔴-6 Tauri CSP（`tauri.conf.json:23` 已设完整 CSP）
    - M-Logic-2 chapter_write ok=false 不抛错（`chapter-tools.ts:73-77` 缺 targetEventId 抛错）
    - M-Logic-9 2s 轮询无退避（`studio.js:157-184` 动态间隔 + 指数退避）
    - M-Logic-12 EventSource 无退避重连（`api-client.js:218-264` 指数退避 1→30s）
    - M-Collab-3 /api/scheduler/mode 全局影响（`routes-scheduler.ts:225-228` 要求活跃项目上下文）
    - M-Qual-1 collect details undefined 静默 resolve（`collect.ts:67` 显式 reject）
    - M29 tauri prebuild 不触发（`tauri.conf.json:9` `beforeBuildCommand` 已设）
- **测试**：
  - 后端：645 全过（基线 644 + 新增 G1-2 active 测例），pass 638 / fail 0 / skipped 7
  - 前端测试轮：`docs/audits/frontend-test-runs/2026-08-04-orch-skeleton.md`，8 项中 5 pass / 1 blocked（BUG-010 派发按钮被拦截，UI 已有问题非本批引入）/ 1 未执行（受 blocked 影响）/ 1 代码审查替代 pass
  - 新发现 1 个 UI bug：BUG-010（登记 `frontend-bug-backlog.md`，open）
- **未列入本批**：G1-4/5/6/8 推到批次 3（编排可见性 + 前端健壮小项）；G2 全部暂不做（用户决策）
