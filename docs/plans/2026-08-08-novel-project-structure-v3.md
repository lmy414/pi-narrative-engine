# 小说工程结构重新约定（v3）需求记录

> 日期：2026-08-08
> 状态：📋 **已记录（2026-08-09 用户定案核心决策：D1 粒度=A、项目定位=小说.json、规则集文件夹+渐进式披露；实施未启动）**——详见 §七 用户决策记录
> 提出人：用户（问题 2/主干结构约定）
> 相关：`docs/novel-project-structure.md`（现行 v2）、`templates/novel/`、`novel/novel.json`、`src/orchestrator.ts`（resolveChapterPath）、`docs/plans/2026-08-08-live-stage-visibility.md`（问题 1 记录）、`docs/plans/2026-08-08-prompt-research.md`（§九 D7-D9 规则集决策）

---

## 一、需求原话与转述

> 用户原话："小说工程结构需要重新约定，区分章节，事件的粒度，让增加额外的文件区域，大纲设定等文本内容。"

**转述**：
1. **重新约定小说工程结构**：现行 v2 结构（`docs/novel-project-structure.md`）在"章节/事件的粒度"上没有清晰且一致的约定，需要重新定义。
2. **区分章节与事件的粒度**：明确"一个章节文件"与"一个事件"各自对应什么存储单元、二者边界如何。
3. **增加额外文件区域**：除正文与规则集外，为**大纲、设定等文本内容**提供约定化的文件区域，引擎与 UI 对其有明确认知。

## 二、现状查证（2026-08-08，事实与矛盾）

### 2.1 现行 v2 结构（文档声称）

```
<novel-root>/
├── novel.json          # name / engine / worldGraphDir / chaptersDir / storyTimeFormat / createdAt
├── 正文/               # 章节文件：第<N>章-<标题>.md（锚点格式）
├── 规则集.md           # 渲染器规则
├── planner 规则集.md   # planner 检索规则
├── 角色规则集.md       # 角色扮演规则
└── .pi/
    ├── world-graph-v3/ # world.db + events.jsonl + memory.md + chapter-index.json + alias-index.json
    ├── scheduler-plans/
    └── ...
```

storyTime 模型：`ch{NNN}.ev{NNN}`——ch=章节号，ev=章内事件序号；同章 ev+1，进新章 ch+1 且 ev 从 001 起；零填充保证字典序==时序。

### 2.2 粒度矛盾（代码 vs 文档 vs 实盘，已查证）

| 层面 | 声称/实际 | 证据 |
|---|---|---|
| 文档约定 | 章节级文件：`正文/第<N>章-<标题>.md`，事件以锚点段落追加进章节文件 | novel-project-structure.md §2；正文/第9章、第11章-未命名.md（文件内 `<!-- event: evt_xxx -->` 锚点分段） |
| **引擎实际** | **每事件一文件**：`chapters/${storyTime}.md`（缺省，硬编码）——`novel.json.chaptersDir` **未被消费** | `src/orchestrator.ts:630` `resolveChapterPath` 缺省 `chapters/${event.storyTime}.md` |
| 实盘数据 | **两种粒度并存**：`正文/第9章、第11章-未命名.md`（章节级） + `chapters/ch012.ev006~ev010.md`（事件级） | `d:\claude\pi-ex\novel\` 实测 |

**结论**：章节/事件的粒度在 v2 中没有被统一约定——文档、模板、引擎缺省、实盘四处不一致。`chaptersDir` 字段形同虚设（只有文档与模板在用），引擎不读它。这正是用户要求"重新约定、区分粒度"的根源。

### 2.3 现状的其他结构问题

- **规则集三件套**：项目根固定文件名（`规则集.md` / `planner 规则集.md` / `角色规则集.md`），引擎按约定文件名加载注入——无"区域"抽象，新增类型的文本内容无处安放。
- **额外文本区域（大纲/设定）**：V6 文件视图技术上已能编辑项目内任意 .md/.txt/.json，但引擎对"大纲""设定"**无认知**——无约定位置、模板不创建、不注入上下文、UI 无分组。
- **结构文档滞后**：novel-project-structure.md §2 目录树未列 `.pi/logs/`、`.pi/sessions/`、`.pi/scheduler-plans/`（实盘已存在）——违反 docs 维护约定 #4 强一致性。
- **chapter-index.json**：世界图侧已维护 `{chapter_id, title, storyTimeRange}` 章节索引，与文件粒度模型脱节（未约定如何随文件粒度变化）。

## 三、目标结构设计方向（v3 草案，供决策）

### 3.1 粒度模型（核心决策）

| 候选 | 模型 | 优点 | 代价 |
|---|---|---|---|
| **A 一章一文件** | `正文/第<N>章-<标题>.md`，事件正文以锚点段落**追加进章节文件**（v2 文档声称的模型） | 阅读单元=章节文件；文件数与章数一致，符合作者直觉；规则集可约束整章结构 | 需保证锚点/追加并发安全（已有 renderToFile 锚点机制）；长章节文件单文件体积增长 |
| **B 一事件一文件** | `正文/ch{NNN}.ev{NNN}.md`（当前引擎缺省行为） | 与 storyTime 一一对应，可追溯粒度最细；文件天然不冲突 | 文件爆炸（一章 N 个文件）；"章节"在文件系统里没有实体（需要章节索引聚合，如 chapter-index.json）；阅读体验碎片化 |
| **C 章目录+事件文件** | `正文/ch{NNN}/ev{NNN}.md` 或 `正文/ch{NNN}.ev{NNN}.md` + 章级 README/汇总 | 兼顾粒度与组织 | 双层级复杂；章级内容如何生成需另约 |

> PM 视角倾向 **A**（对齐"正文=章节文件"的作者直觉，章节文件即阅读与交付单元，事件是章内段落粒度）或 **B 显式化**（若作者要事件级追溯，则明确"一章=一目录/一索引"）。最终由用户定。

### 3.2 novel.json v3 schema 草案

```jsonc
{
  "name": "{{name}}",
  "engine": "narrative-engine",
  "engineVersion": "{{engineVersion}}",
  "worldGraphDir": ".pi/world-graph-v3",
  "storyTimeFormat": "ch{NNN}.ev{NNN}",
  "chaptersDir": "正文",                    // 语义与粒度模型绑定（章节级文件 or 事件级文件目录）
  "contentAreas": {                        // 新增：额外文本区域（键=区域名，值=相对目录）
    "outline": "大纲",                     //   大纲（卷纲/章纲）
    "setting": "设定",                     //   设定（世界观/人物/地点/物品/时间线）
    "archive": "素材"                      //   可选：素材/随笔等自定义区域
  },
  "createdAt": "{{date}}"
}
```

- 兼容性：v2 工程缺省 `contentAreas` 时仅保留 `正文`，不破坏既有项目；未知区域键保留写入（`@pi/admin` read/writeNovelJson 已允许未知扩展字段）。
- 区域文件默认约定：`大纲/README.md`（区域说明）+ 可选 `大纲/卷纲.md`、`设定/世界观.md` 等模板文件，脚手架一并创建。

### 3.3 引擎与 UI 对区域的认知（分步落地候选）

| 能力 | 说明 | 依赖 |
|---|---|---|
| 模板/脚手架 | 创建 `大纲/`、`设定/` 空目录 + 引导 README | 独立可做 |
| V6 文件视图 | 按 contentAreas 分组展示文件树（区域标题），非 .md 区域照常可编辑 | 独立可做 |
| 规则集引用 | 渲染/角色/规划规则集可显式引用区域文件（如"按 设定/人物卡.md 保持人设"） | 注入方式待定 |
| **上下文注入** | 大纲/设定摘要注入主会话与子代理上下文（前情与设定一致性的数据源） | 与 G5-1 记忆注入强关联，建议随 G5 一起做 |
| 渲染器 | 章节文件从"正文"或指定区域读取上下文（当前 renderToFile 把整个章节文件当上下文） | 依赖粒度模型 D1 |

### 3.4 现有 novel/ 工程收敛（D1 决策后的清理）

实盘 `novel/` 同时存在 `正文/第9、11章`（章节级）与 `chapters/ch012.evNNN`（事件级）——决策粒度后需二选一收敛：合并事件文件回章节文件（需按 chapter-index 或 storyTime 归组），或反向拆分；迁移前备份。此收敛亦修正 `resolveChapterPath` 的缺省路径与 `chaptersDir` 的消费关系。

## 四、开放决策点（实施前需对齐，本记录不选型）

| # | 决策点 | 候选方向 | 2026-08-09 状态 |
|---|---|---|---|
| D1 | **章节/事件粒度模型** | A 一章一文件+事件锚点段落（推荐） / B 一事件一文件+章节索引 / C 章目录+事件文件 | ✅ **已定案：A**（§七 D10） |
| D2 | **区域声明方式** | novel.json `contentAreas` 映射 vs 固定约定目录（大纲/、设定/）vs 两者结合 | ✅ **已定案：固定约定目录为主**（§七 D10：笔记/草稿/设定/大纲；小说.json 映射与否待细化） |
| D3 | **区域文本注入 LLM 上下文** | 不注入（仅作作者资料区） / 摘要注入 / 规则集显式引用 | ❌ 未决 |
| D4 | **chaptersDir 语义修正** | 与 D1 绑定：章节级文件目录（A）或事件级文件目录（B/C）；`resolveChapterPath` 缺省值改为消费 chaptersDir 并校验存在 | ✅ **随 D1=A 绑定**（正文/ = 章节级文件目录） |
| D5 | **现有 novel/ 收敛策略** | 合并 chapters/ 事件文件回 正文/ 章节文件 / 反向拆分 / 保持并存仅文档声明（不推荐） | ❌ 未决 |
| D6 | **规则集是否纳入区域模型** | 三件套留在项目根（现状，最省） / 迁入 `规则/` 区域并兼容读取旧位置 | ✅ **已定案：规则集文件夹**（§七 D11：文风规则.md/检查规则.md/自定义规则.md） |

> 注：D7-D9 为提示词工程决策（planner/角色/渲染规则集归属），见 `prompt-research.md` §九，与 D6 联动。

## 五、验收标准（草案）

1. 结构文档 v3 与代码、模板、实盘一致（消灭 §2.2 四处矛盾），`chaptersDir` 被引擎真实消费。
2. 新工程脚手架创建全部区域目录（正文/大纲/设定 + 引导 README）。
3. 旧工程（缺 contentAreas）打开不破坏、可编辑、可创作。
4. 章节/事件粒度唯一约定：文件系统布局可由 storyTime 无损推导（或反之）。
5. 大纲/设定文件可被 V6 编辑，且至少一种机制（规则集引用或上下文注入）能影响生成。

## 六、备注

- 本记录与问题 1（编排实时可见性）独立；与 G5-1（记忆注入）有依赖交集（D3）。
- 记录人：ZCode（2026-08-08，按问题 1 相同流程：先记录，不实施、不改契约与代码）。

## 七、用户决策记录（2026-08-09）

> 状态：📋 **已记录（结构核心已定案，实施未启动）**——仅记录决策，不改代码、不建目录。
> 编号沿用 prompt-research.md §九 的 D7-D9（规则集决策），本结构决策从 D10 起。

### D10｜新小说项目结构定案（2026-08-09）

**决策原话**：

> 一个小说项目就是一个文件夹，引擎通过识别小说.json文件定位。
> 文件夹下包含初始的：
> 顶层：小说.json（项目配置文件） Reme.md（项目简介）
> 正文文件夹：
> 第一章：（每个事件不单独创建MD文件，一章为一个.md，章节内事件用锚点区分）
> 第二章：
> 第三章：
> 运行时数据（数据库等内容）
> 其他内容文件夹：笔记，草稿，设定，大纲，等内容文件（用户和代理都可以编辑的）

**定案解读**：
1. **项目定位**：一个项目 = 一个文件夹；**引擎通过识别 `小说.json`（项目配置文件）定位项目**（现状为 `novel.json`——改名存疑，见待确认项）
2. **粒度模型 = A 定案**（对齐 §三.3.1 候选 A）：**一章一个 .md，章节内事件用锚点区分**（`<!-- event: <eventId> -->` 锚点机制已存在于 `@pi/renderer` chapter-edit；storyTime `ch{NNN}.ev{NNN}` 体系保留）
3. **顶层**：`小说.json`（项目配置）+ `Reme.md`（项目简介——拼写待确认，见待确认项）
4. **正文文件夹**：`第一章/第二章/第三章...`（一章 = 一个 .md 文件，文件名细节待实施细化，如 `第N章.md` 或 `第N章-标题.md`）
5. **运行时数据**：数据库等（现状 `.pi/`：world-graph-v3 / scheduler-plans / sessions / logs）
6. **其他内容文件夹**：`笔记/` `草稿/` `设定/` `大纲/` 等——**用户和代理都可编辑**（D3 注入机制未决）
7. **规则集文件夹**：见 D11

**待确认项**：
- **`小说.json` vs 现状 `novel.json`**：v3 是否正式改名（含迁移与兼容读取旧文件）
- **`Reme.md` 拼写**：疑似 `README.md` 笔误，按原文记录，实施时确认
- **章节文件命名**：`第一章.md` / `第1章.md` / `第N章-标题.md`（storyTime 关联规则）
- **"等"内容文件夹**：是否开放自定义区域（结合 D2 的 contentAreas 映射与否）
- **D3 区域注入机制、D5 现有 novel/ 收敛**：仍未决

### D11｜规则集文件夹 + 渐进式披露加载（2026-08-09）

**决策原话**：

> 规则集文件夹：
> 文风规则.md
> 检查规则.md
> 自定义规则.md
> 采用Skill的渐进式披露机制加载规则集到渲染器内。

**定案解读**：
1. **规则集迁入文件夹**（D6 定案）：`规则集/` 目录下 `文风规则.md`、`检查规则.md`、`自定义规则.md`；现行三件套（规则集.md / planner 规则集.md / 角色规则集.md）退出
2. **归属（联动 prompt-research §九 D7-D9）**：
   - 文风规则.md = **外部可编辑**（D9："只保留文风给外部编辑"）
   - 检查规则.md = checker（render_check）用；归属未明说（引擎自维护？）
   - 自定义规则.md = 性质未明说（用户/代理可写？预留扩展？）
   - planner 检索规则、角色扮演规则：已收回引擎自维护（D7/D8），不再以外部文件存在
3. **加载机制 = Skill 渐进式披露**（pi SDK/hanako 的 SKILL.md 机制）：渲染器提示词中不全文注入规则集，而是列出可用规则（名称+简介+位置），渲染器**按需读取全文**（需渲染器侧提供"读取规则文件"能力，参考 `lib/skills/skill-metadata.ts` 的 `<available_skills>` XML + 懒加载 read 模式；hanako-reference §2.3/A5 工具自描述三档）

**待确认项**：
- 检查规则.md / 自定义规则.md 的编辑归属（文风之外是否一律引擎自维护）
- 渐进披露的"按需读取"工具形态（复用 chapter_read 类工具 vs 专用 rules 读取工具）
- 规则集文件夹目录名（`规则集/` 中文名，与正文/笔记/大纲一致）

**关联**：D6 定案；`prompt-research.md` §九 D7/D8/D9（规则集归属决策链）；§5.5 RN1（renderRuleSet 注入缺口修复）实施时与规则集文件夹+渐进披露一并设计。

### D12｜git 管理规则与数据库方案（2026-08-09）

**决策原话**：然后加一个git管理的内容规则正文之类的，只不过数据库不知道怎么管理。

**决策解读**：
- 结构 v3 需含 **git 管理规则**：明确哪些内容进 git（创作资产），哪些排除（运行时数据）
- **数据库（world.db 二进制）git 管理困惑**：SQLite 二进制无法 diff/merge、每次运行都变、冲突只能覆盖——用户提出此困惑，方案见下

**现状查证（2026-08-09，novel/ 实盘）**：
- `.gitignore` 现状：已忽略 node_modules/*.log/.DS_Store/Thumbs.db/`*.db-shm`/`*.db-wal`/`.pi/scheduler-plans/`/`.mimosa/`
- **但 git 实际已跟踪**：`.pi/world-graph-v3/` 下 5 个顶层文件——`world.db`（9.8MB 二进制）、`events.jsonl`（33KB 文本事件日志）、`_v3_dump.json`（112KB 导入基线快照）、`alias-index.json`、`chapter-index.json`；**以及 5 个 `.mimosa/` hook 状态文件**（工具痕迹，不应入库）
- 未跟踪（git status 显示）：`.pi/logs/`、`.pi/sessions/`、`chapters/`（事件级文件）、`正文/第11章-未命名.md`
- **事实链**：`events.jsonl` 是 append-only 权威事件日志（EventLog，每行一个 EventRecord）；`world.db` 是其物化投影；**但 underworld-graph 0.3.1 无"从 events.jsonl 重放重建 world.db"API**（`src/world-graph.ts` 仅 create/migrate）；`_v3_dump.json` 是 novel-importer 导入基线（一次性，不随剧情更新）

**git 管理规则（v3 定案）**：

| 类别 | 内容 | git |
|---|---|---|
| 创作资产（文本） | `小说.json`、`Reme.md`、`正文/`（章节 md）、`规则集/`、`笔记/ 草稿/ 设定/ 大纲/` | ✅ 入库 |
| 世界图文本（可 diff） | `.pi/world-graph-v3/events.jsonl`（剧情权威日志）、`alias-index.json`、`chapter-index.json`、导入基线 dump | ✅ 入库 |
| 运行时数据 | `.pi/world-graph-v3/world.db`（+ shm/wal）、`.pi/sessions/`、`.pi/logs/`、`.pi/scheduler-plans/` | 🚫 排除 |
| 工具痕迹 | `.mimosa/`（hook 状态等） | 🚫 排除 |

**数据库方案（用户困惑的答案，三选项）**：

| 方案 | 做法 | 代价 | 推荐 |
|---|---|---|---|
| **A 现状维持** | world.db 继续入库；仅清理误入库的 `.mimosa/`、补 gitignore（sessions/logs/*.db） | db 频繁变更、git 历史膨胀；恢复依赖 db 本体 | 备选 |
| **B 文本快照替代（推荐）** | world.db gitignore；引擎补"当前世界状态导出 JSON"小能力（参照 `_v3_dump.json` 生成器改造为持续快照），提交点导出快照入库；db 丢失可从快照+events.jsonl 恢复 | 需引擎加导出命令（小成本） | ✅ |
| **C 事件重放（长期）** | underworld-graph 补"从 events.jsonl 重放重建 world.db"API，events.jsonl 完全替代 db 入库 | 上游改造（与"写接口补齐回上游"待办同线） | 远期 |

**实施修正项（实施时执行）**：
1. `.gitignore` 补充：`*.db`、`.pi/logs/`、`.pi/sessions/`（`*.db-shm`/`*.db-wal` 已有）
2. `git rm --cached` 移除已入库的 `.mimosa/` 文件与（若选 B）`world.db`
3. 方案 B 落地时：引擎加世界状态导出（复用 underworld-graph 查询接口，输出 JSON），提交纪律"每完成一章提交 = 正文 + events.jsonl + 状态快照，等于剧情存档点"

**待确认项**：
- 数据库方案选 A / B / C（建议 B）
- 提交节奏约定（每章一提交？里程碑提交？）
