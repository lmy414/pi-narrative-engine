# 前端测试轮 · 2026-08-05-fix-bug20-17

## 元信息
- 触发原因：修复 BUG-020（移除输入区 4 个工具按钮）+ BUG-017（切换会话后 live 绿点不联动）
- 受影响页面：studio（#/studio）输入区 / 会话列表
- 服务地址：http://127.0.0.1:7421
- 执行者：AI（browser_use）
- 开始时间：2026-08-05

## 测试清单

### 页面：studio 输入区（BUG-020）
- [x] TC-001 输入区不再显示 4 个工具栏按钮（附件/加粗/斜体/列表），仅保留 hint「Enter 发送 · Shift+Enter 换行」（功能：验证按钮移除）
  - 步骤：打开 #/studio，观察输入区工具栏
  - 预期：无 4 个图标按钮，仅见 hint 文本
  - 实际：browser_snapshot 确认输入区无 4 个图标按钮，仅见 hint「Enter 发送 · Shift+Enter 换行」
  - 结果：✅
- [x] TC-002 输入区无 JS 报错（删除 stToolAction 后无悬挂引用）（控制台：验证删除无残留调用）
  - 步骤：打开 #/studio，采集 console 消息
  - 预期：无 "stToolAction is not defined" 类错误
  - 实际：console 仅含 two.js/three.js 废弃警告，无 stToolAction 或 JS 报错
  - 结果：✅
- [x] TC-003 输入框可正常输入并发送（回归：按钮移除不影响核心发送链路）（功能）
  - 步骤：输入文本，回车发送
  - 预期：消息出现在聊天区
  - 实际：输入「前端测试发送一条消息」回车，消息出现且输入框清空
  - 结果：✅

### 页面：studio 会话列表（BUG-017）
- [x] TC-004 切换会话后 live 绿点跟随当前活动会话（功能：验证 live 字段刷新）
  - 步骤：打开 #/studio（mock 模式），存在多个会话时点击切换到另一会话
  - 预期：绿点（.st-live-dot）转移到刚切换的会话项，旧会话绿点消失
  - 实际：8 个会话，点击 idx3 会话后 browser_evaluate 确认 live=true + active 均转移到该会话，旧会话 live=false
  - 结果：✅
- [x] TC-005 切换会话后消息区内容正确切换（回归：切换核心逻辑不受影响）（功能）
  - 步骤：切换会话后观察聊天区
  - 预期：聊天区显示目标会话消息
  - 实际：切换后聊天区显示目标会话 28 条消息，active 会话持有绿点
  - 结果：✅
- [x] TC-006 会话列表渲染无 console 报错（控制台）
  - 步骤：切换会话，采集 console 消息
  - 预期：无 JS 报错
  - 实际：切换后 console 无新增 JS 报错
  - 结果：✅

## 缺陷登记

| 编号 | 所属项 | 严重度 | 复现步骤 | 期望 | 实际 | 截图 | 状态 |
|------|--------|--------|---------|------|------|------|------|
| （无） | - | - | - | - | - | - | - |

## 小结
- 通过 6 项 / 失败 0 项 / 跳过 0 项
- 缺陷分布：P0 0 / P1 0 / P2 0 / P3 0
- 总体评价：两个修复点均验证通过，无回归。BUG-020 按钮移除干净（无死引用），BUG-017 绿点切换后正确转移。