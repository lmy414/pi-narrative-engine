# 前端测试轮 · 2026-08-03-fix-bug8-9

> **本轮验证**：BUG-008（提交按钮无反馈 → withLoading 全局 loading 覆盖层）与
> BUG-009（设置页响应慢 → settingsLoad 并行化 + version/doctor 懒加载 + compareVersions 超时）。
> 引擎侧 `compareVersions` 超时由 packages/admin/tests/updater.test.ts 新增 2 项单测覆盖（11/11 绿），
> 本轮为浏览器实操清单。

## 元信息
- 触发原因：修复 frontend-demo（app.js / views/settings.js / styles/components.css）后按前端测试纪律自驱测试轮
- 受影响页面：全局（loading 覆盖层）/ 设置页（并行化 + 懒加载）
- 服务地址：http://127.0.0.1:7421（`node scripts/app-server.mjs --port 7421 --embed`，修改后重启加载新代码）
- 激活项目：测试项目A（test-novel-a）
- 执行者：AI（playwright-core + Edge）
- 开始时间：2026-08-03
- 截图存档：`shots/2026-08-03-fix-bug8-9/`

## 测试清单

### BUG-008 loading 覆盖层（withLoading 视觉反馈 + 防重复点击）
- [x] TC-001 长操作（800ms）期间覆盖层可见，且 pointer-events 拦截点击 ✅
- [x] TC-001b 操作结束覆盖层自动隐藏 ✅
- [x] TC-002 短操作（50ms）不闪烁（150ms 延迟窗口）✅
- [x] TC-003 withLoading 期间 `App.loading=true` 标志保持 ✅

### BUG-009 设置页并行化 + 懒加载
- [x] TC-004 设置页首屏渲染 140ms（此前串行等 version 需 4.5~14s）✅
- [x] TC-005 about 面板懒加载后显示本地版本（v— → v0.1.0-alpha.1）✅
- [x] TC-005b 环境自检 12 项懒加载渲染 ✅
- [x] TC-006 手动「检查更新」按钮有 toast 反馈（已是最新版本）✅
- [x] TC-007 手动「运行检查」按钮有 toast 反馈（12 项通过 / 0 警告 / 0 失败）✅
- [x] TC-008 规则集面板并行加载后内容齐全（编辑器 + 内容）✅
- [x] TC-008b 环境变量面板加载正常 ✅
- [x] TC-009 设置页二次进入仍快速（13ms）✅

### 服务端 version 超时兜底（Node 直测）
- [x] TC-010 /api/admin/version 200 + 本地版本 + 耗时受 5s 超时约束（4054ms，remote=null 弱网场景）✅

## 缺陷登记

| 编号 | 所属项 | 严重度 | 复现步骤 | 期望 | 实际 | 截图 | 状态 |
|------|--------|--------|---------|------|------|------|------|
| （无） | — | — | — | — | — | — | — |

> 本轮 0 缺陷。执行中发现 2 个测试脚本自身问题（非产品缺陷），均已修正：
> 1. toast 生命周期仅 3s，TC-006/007 固定等 7s 后读取必然为空 → 改轮询捕获（每 300ms 检查，上限 9s）；
> 2. 类名不匹配（非缺陷）：`.set-slot-card`（models 面板实际类名不同）、`.set-env-row`（env 面板行类名不同）→ 改用文本长度兜底断言。
> 说明：真实「提交」按钮未点击——commit 会触发 LLM 渲染写正文、耗时不可控且污染测试项目；
> stCommitPlan 走 withLoading，其覆盖层行为已由 TC-001/001b 机制验证覆盖。

## 小结
- 通过 13 项 / 失败 0 项 / 跳过 0 项
- 缺陷分布：P0 0 / P1 0 / P2 0 / P3 0
- 总体评价：设置页首屏由串行 4.5~14s（git ls-remote 网络阻塞）降至 140ms 并行渲染；version/doctor 后台懒加载，到达后仅 in-about 时自动刷新，不打断其他面板编辑；手动检查按钮回归正常；withLoading 覆盖层修复了提交等重操作零反馈问题并兼防重复点击。
