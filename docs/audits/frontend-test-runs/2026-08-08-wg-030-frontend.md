# 前端测试轮：WG 0.3.0 声明列表改造（2026-08-08）

> 日期：2026-08-08
> 触发：narrative-engine 阶段 4 前端改造（underworld-graph 0.3.1 适配），按前端测试纪律自驱测试轮
> 服务：`node scripts/app-server.mjs --project D:/claude/pi-ex/frontend-test-project --port 7422`
> **偏差记录**：纪律约定 7421，但 7421 被升级前启动的旧实例（PID 20792，服务 novel 旧库）占用，本轮用 7422
> 测试数据：种子脚本 `scripts/seed-frontend-test-project.mjs`（8 实体 / 13 事件 / 8 关系，中文词表 property + relation description + belief 跨实体声明）
> 状态：✅ **已跑完（2026-08-08）**——14/15 通过，1 项 N/A，缺陷 1 待查证 + 2 项 P2 增强登记

## 产出清单

| # | 测试项 | 操作路径 | 预期 | 结果 |
|---|---|---|---|---|
| 1 | 实体列表中文名 | 世界图页左侧列表 | 显示 Entity.name 快照（林远航/艾莉亚…），非 entityId | ✅ PASS |
| 2 | 搜索 name 直读 | 世界图搜索框输入「林远航」 | 命中 ent_char_lin + 老陈（摘要命中），结果显示中文名 | ✅ PASS |
| 3 | Inspector 属性声明列表 | 点击实体 → Inspector | 属性区显示 property + description + belief 徽标，「名字」声明不重复 | ✅ PASS（信念.关于_林远航.目的 行带 belief 徽标） |
| 4 | Inspector 关系行 | 同上 | 关系对端中文名 + label | ✅ PASS |
| 5 | 详情属性面板声明列表 | 实体详情 → 属性 tab | 声明列表（description + modality 徽标），名字声明剔除 | ✅ PASS |
| 6 | 详情声明 tab 全量历史 | 实体详情 → 声明 tab | 全部声明（含已闭合 + validFrom→validTo + 闭合原因 + modality 标签） | ✅ PASS（含已闭合「心情=警惕」卡） |
| 7 | 编辑属性 description 契约 | 编辑「心情」→「激昂」 | 旧声明闭合、新声明生效 | ✅ PASS（API 回查 心情=激昂） |
| 8 | 添加属性 | 添加「称号」=「星海旅人」 | 新声明出现 | ✅ PASS |
| 9 | 可见性矩阵 description | 可见性 tab | 行名显示「属性 = description」 | ✅ PASS |
| 10 | 事件链 newFacts description | 事件链页 | 新事实显示 description（中文），实体名中文 | ✅ PASS（林远航.位置 = 迷雾星云） |
| 11 | 快速记事件 birth 新实体 | birth + 实体名称「测试新角色」 | 创建后 Entity.name 快照自动同步 | ✅ PASS（API 回查 name=测试新角色） |
| 12 | 快速加关系 | 认识 / ent_char_test01→ent_char_lin | 关系创建成功 | ✅ PASS |
| 13 | 关系 description 展示 | Inspector 关系行 / 关系 tab | 叙事描述可见 | ⚠️ 未实现 → 登记 P2-2（无展示位） |
| 14 | 历史 facts description | 声明 tab（全量历史） | facts 显示 description | ✅ PASS（T6 覆盖） |
| 15 | 全局渲染无 JS 报错 | 各页切换 | console 无 TypeError | ✅ PASS（仅 three.js 既有弃用警告 + 服务重启竞态期的 2 条历史 ERR_CONNECTION_REFUSED） |

## 缺陷登记

（本轮发现的缺陷，禁止即修；汇总后交用户决策）

| 编号 | 等级 | 描述 | 操作复现 | 处置 |
|---|---|---|---|---|
| F-1 | 待查证（中） | 世界图搜索框**清空输入后实体列表不恢复全量**：输入「林远航」过滤后，清空（fill('') / Backspace）列表仍残留 2 条过滤结果，需点「全部」类型筛选按钮才恢复；3D 节点却已恢复（列表/3D 状态不同步） | ① 搜索框输入「林远航」② 清空输入 | ✅ 已查证+已修（2026-08-08）：渲染层 matchesFilter 按 searchFilter 过滤导致 DOM 行集合不完整 → 移除渲染层搜索过滤，保留 DOM 级即时过滤；清空恢复全量（浏览器复验 9 行全恢复） |
| P2-1 | 增强（低） | 快速加关系表单无 description 输入框（后端已支持，前端未暴露） | 快速加关系弹窗 | ✅ 已补（2026-08-08）：表单加「关系描述（可选）」+ api-client addRelation 第 5 参透传 |
| P2-2 | 增强（低） | 关系 description 无前端展示位（Inspector 关系行/关系 tab 仅显示 label） | 世界图 Inspector 关系区 | ✅ 已补（2026-08-08）：Inspector rel-row 副行 + 详情关系卡 description 行 + CSS |

## 汇总

- **通过 14/15**，N/A 0（T13 登记为 P2 增强）；0.3.0 声明列表改造核心链路全部走通：name 快照直读、description 契约读写、belief modality 徽标、同 property 历史声明展示、birth「名字」→快照自动同步。
- **缺陷 3 项**：F-1 待查证（搜索清空不重置，疑似既有交互缺陷或本轮 name 拼接改动引入——需代码查证）；P2-1/P2-2 为新增 description 字段的前端消费缺口（后端契约已就绪）。
- **测试环境备注**：种子脚本首版把 belief 声明写成 fact（夹具 bug，已修重播）；7421 端口被旧实例占用（升级前启动，仍在服务 novel 旧库，其数据为 0.1.2 旧形状——注意该实例在引擎升级后行为未验证，阶段 5 会处理 novel 旧库）。
