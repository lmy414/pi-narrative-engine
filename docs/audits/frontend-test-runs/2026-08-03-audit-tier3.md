# 前端测试轮 · 2026-08-03-audit-tier3

> **本轮验证**：2026-08-03-code-audit 第三梯队（M 级 + L 级）中影响前端的部分 ——
> M-Qual-3（debug 页渲染上限 500 + 截断提示）、L-FE-1（app.js 移除 App.loading 死标志）、
> L-FE-2（事件页实体标签委托 DemoUtils.eventEntityIds）、L-FE-5（api-mock getChain 环检测）、
> L-FE-3/4（visualizer-ui API 统一重构，运行时验证受限见下）、M-Collab-4（扫描根白名单）。
> 引擎侧 M-Logic/M-Sec/M-Qual 等其余项由 tests/*.test.ts 覆盖（297 项全绿），不在本轮浏览器清单。

## 元信息
- 触发原因：tier3 批次修改 frontend-demo/ 及其直接依赖（app.js / views/*.js / api-mock.js / demo-utils.js / styles），按前端测试纪律自驱测试轮
- 受影响页面：首页 / 项目页 / 事件页 / 调试页 / 图页 / 设置页
- 服务地址：http://127.0.0.1:7421（`node scripts/app-server.mjs --port 7421`，每轮改动后重启加载新代码）
- 执行者：AI（playwright-core + Edge，脚本 `%TEMP%\opencode\pwt\run-audit-tier3.js`）
- 开始时间：2026-08-04
- 截图存档：`shots/2026-08-03-audit-tier3/`

## 测试清单（最终轮 11/11）

### L-FE-1 withLoading 简化回归
- [x] TC-001 首页加载且活跃项目条正常渲染 —— **环境状态问题**（见缺陷登记 D-02）
- [x] TC-002 项目页扫描流程（withLoading 简化无死锁）✅（卡片 3 张，覆盖层无残留）
- [x] TC-002b 「进入」跳转图页（loading 覆盖层无残留）✅

### L-FE-2 事件页实体标签委托
- [x] TC-003 事件页正常渲染（78 张卡片）✅
- [x] TC-004 事件卡片实体标签渲染（eventEntityIds 委托 demo-utils 后仍正常，428 个实体标签）✅

### M-Qual-3 调试页渲染上限
- [x] TC-005 渲染上限提示逻辑（数据 <500 条时不渲染截断提示，dbg-workspace 正常）✅

### L-FE-5 事件链环检测 / 图页
- [x] TC-006 图页 3D 正常渲染（SRI 修复后 canvas 恢复）✅
- [x] TC-007 事件链下钻无递归异常（getChain 环检测；mock 无环）✅

### 设置页 / visualizer-ui / 全站
- [x] TC-008 设置页正常渲染 ✅
- [x] TC-009 visualizer-ui 壳层 —— **不可达属设计现状**（见缺陷登记 D-03）
- [x] TC-010 全站无 JS 报错（SRI 修复后仅剩 2 个无害 404）✅

## 缺陷登记

| 编号 | 所属项 | 严重度 | 复现步骤 | 期望 | 实际 | 状态 |
|------|--------|--------|---------|------|------|------|
| D-01 | M-Collab-4 | P1 | app-config 设置 `launcher.defaultScanRoots=[novel]` 后 `GET /projects/scan?root=<novel>` | 200（根在白名单内） | 403 SCAN_ROOT_NOT_ALLOWED——`relative()` 返回 `""`（相等）时 `rel !== ""` 判 false，**仅放行子目录、拒绝根本身** | **已修复**（routes-ext.ts 改为 `rel === ""` 放行 + 补测试「根本身放行 / 越界 403」unified-server.test.ts 64/64） |
| D-02 | 环境状态 | — | 服务重启后打开首页 | 活跃项目条显示上次项目 | App.activeProject 为空——活跃项目为服务端内存态，重启即失；defaultScanRoots 指向 novel 本体（非容器）时扫描 0 结果 | 非本轮回归，登记备忘：dev 环境可把 scanRoot 指向容器目录（`D:\claude\pi-ex`，验证出 3 卡片） |
| D-03 | 设计现状 | — | 访问 /visualizer/ | — | 404——unified-server 只伺服 frontend-demo（resolveDefaultUiDir 优先 frontend-demo，visualizer-ui 仅旧版回退）；AGENTS.md 描述与此不一致，登记存疑 | L-FE-3/4 运行时验证受限（其代码变更仅在 tauri snapshot 场景生效） |
| D-04 | 环境 | — | SRI 校验 | — | vendor 4 个库全 blocked：`core.autocrlf=true` 使 git checkout 把 vendor 文件转为 CRLF，字节变化导致 integrity 失败 | **已修复**：新增 `.gitattributes`（`frontend-demo/vendor/* -text`）+ 还原 LF；复测 SRI 通过、3D 恢复 |

## 小结
- 通过 10 项 / 失败 1 项（TC-001 为环境状态问题，非回归）/ 缺陷 4 条
- 缺陷分布：P0 0 / P1 1（D-01 已修复）/ 环境与设计现状 3 条（D-02/03/04，D-04 已修复）
- 总体评价：tier3 前端改动零回归；测试轮暴露 1 个真实产品缺陷（M-Collab-4 白名单语义 bug）与 1 个工程隐患（vendor CRLF 污染 SRI），均已修复并补测试/防回归。
